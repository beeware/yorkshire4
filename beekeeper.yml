pull_request:
    - beefore:
        name: Prebuild checks
        subtasks:
            - pycodestyle:
                name: Python lint checks
                task: beefore
push:
    - beefore:
        name: Prebuild checks
        subtasks:
            - pycodestyle:
                name: Python lint checks
                task: beefore
